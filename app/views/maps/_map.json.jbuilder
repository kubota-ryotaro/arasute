json.extract! map, :id, :station_name, :large_category, :small_category, :store_name, :address, :latitude, :longitude, :business_hours, :regular_holiday, :created_at, :updated_at
json.url map_url(map, format: :json)
